# Example environment variables for local development and production
DEBUG=False
SECRET_KEY=replace-me-with-a-secret
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
# For production set to your DB connection, e.g. postgres://user:pass@host:port/dbname

# Any other env vars used by your app
